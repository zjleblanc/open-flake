# Example TLS secret — replace placeholder values before applying.
# Create from real certificates:
#   kubectl create secret tls openflake-tls \
#     --cert=fullchain.pem --key=privkey.pem
#
# Or with cert-manager, omit this file and use the Ingress annotation instead.
apiVersion: v1
kind: Secret
metadata:
  name: openflake-tls
type: kubernetes.io/tls
data:
  tls.crt: LS0tLS1CRUdJTi...  # base64-encoded certificate (replace me)
  tls.key: LS0tLS1CRUdJTi...  # base64-encoded private key (replace me)
